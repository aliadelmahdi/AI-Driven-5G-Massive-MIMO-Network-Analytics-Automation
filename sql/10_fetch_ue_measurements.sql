SELECT
  timestamp_utc,
  ue_id,
  serving_cell_id,
  beam_id,
  rsrp_dbm,
  rsrq_db,
  sinr_db,
  cqi,
  rank_indicator,
  mcs_index,
  bler_dl_pct,
  timing_advance,
  dl_throughput_mbps,
  ul_throughput_mbps,
  handover_event,
  velocity_kph,
  packet_loss_pct,
  latency_ms,
  jitter_ms,
  harq_retx_pct,
  device_profile,
  service_type,
  traffic_load_mb,
  neighbor_cell_count,
  beam_switch_count
FROM ue_measurements;
